import type { MemoryAttachmentEvidence } from "../types.js";
/** Validate persisted/captured evidence with a budget independent of chat text. */
export declare function normalizeAttachmentEvidence(value: unknown): MemoryAttachmentEvidence[];
